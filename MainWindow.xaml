<Window x:Class="rbxml_convert.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RBXMLConvert WPF Application" MinHeight="200" MinWidth="300"
        MaxHeight="250" MaxWidth="350">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Thank you for using my conversion tool!"
                   Margin="10" FontSize="16" FontWeight="Bold"/>

        <Label Content="Selected File Path:" Grid.Row="1"/>
        <TextBox x:Name="FilePathTextBox" Margin="10" Grid.Row="2" IsReadOnly="True"/>

        <Button Content="Convert" Click="ConvertButton_Click" Width="100" Height="30" Margin="10" Grid.Row="3"/>

        <TextBlock x:Name="ResultTextBlock" Margin="10" Grid.Row="4"/>
    </Grid>

</Window>
